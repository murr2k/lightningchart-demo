# LightningChart Demo - Environment Variables
# 
# This is a sample environment file for developers who fork this repository.
# Copy this file to .env and replace the placeholder values with your actual credentials.
# NEVER commit the .env file to version control!

# Fly.io Configuration
# Get your token from: https://fly.io/user/personal_access_tokens
FLY_API_TOKEN=your_fly_api_token_here
FLY_ORG=your-fly-organization

# Cloudflare Configuration (for custom domain)
# API Token: https://dash.cloudflare.com/profile/api-tokens
# Zone ID: Found in your domain's overview page on Cloudflare
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
CLOUDFLARE_ZONE_ID=your_cloudflare_zone_id
CLOUDFLARE_DNS_RECORD_ID=dns_record_id_for_subdomain

# GitHub Configuration (optional, for deployment status updates)
# Create a token at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token

# Application Configuration
NODE_ENV=production
APP_NAME=lightning-chart
PRIMARY_REGION=iad
CUSTOM_DOMAIN=lc.linknode.com

# Optional: Monitoring and Analytics
SENTRY_DSN=your_sentry_dsn_if_using_error_tracking
ANALYTICS_ID=your_analytics_id_if_applicable

# Optional: NPM Registry (if using private packages)
NPM_TOKEN=your_npm_token_if_needed

# Development Settings (local only)
DEV_PORT=5173
DEV_HOST=localhost

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in the .env file
# 3. Never commit .env to git (it's in .gitignore)
# 4. For production, set these as environment variables in your deployment platform
#
# For Fly.io secrets:
# flyctl secrets set FLY_API_TOKEN=your_token
#
# For GitHub Actions secrets:
# Add via Settings > Secrets and variables > Actions
#
# Security Note:
# Keep your credentials secure and rotate them regularly.
# If you accidentally commit credentials, revoke them immediately and generate new ones.